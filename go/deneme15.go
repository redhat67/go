package main

func deneme15() {

	/* fmt.Println(result(40))
	}

	func result(grade int) string {
		if grade >= 50 {
			return " geçtiniz"

		}
		return "kaldınız"

	}
	*/
	/* **************************************************************** */
	/* başlat("Atamer", 20)

		name := "Atamer"
		age := 20
		fmt.Printf("Adım %s, yaşım %d\n", name, age)

	}
	func başlat(name string, age int) {
		fmt.Printf("Adım %s yaşım %d", name, age)

	} */

	/* ************************************************************** */
	/*
		var moment time.Time = time.Now() // şuanki zamanın çıktısını verir
		fmt.Println(moment)
	/* ****************************************************************************** */
	/* fmt.Print("Lütfen sınav sonucunu giriniz: ")
	reader := bufio.NewReader(os.Stdin) // bufio = paket newReader = method // bu satır klavye den yazdırma satırıdır.
	value, _ := reader.ReadString('\n') // _ alt tire nin anlamı blank identifier // bu satırda klavyeden yazdıracağımız değeri string olarak atıyoruz.
	fmt.Println(value) */
	/* *********************************************************************************** */
	/* 	bölüm, kalan := bölme(104, 5)
		fmt.Println(bölüm, kalan)
	}

	func bölme(bölünen int, bölen int) (bölüm, kalan int) {
		bölüm = bölünen / bölen
		kalan = bölünen % bölen

		return bölüm, kalan
	} */
}
