package main

func deneme13() {
	/*
		for i := 1; i <= 10; i++ {
			if i%2 == 0 {
				fmt.Println(i, "Çifttir") // if yapısıyla tek çift olarak yazdırma
			} else {
				fmt.Println(i, "tektir")
			}
		} */
	/* **************************************************************************************** */
	/* x := 0
	for x < 10 {
		fmt.Println(x) // for yapısını while döngüsü ile yazmak
		x++
	} */
	/* ******************************************************************************************************* */

	/* switch x := 75; {
	case x < 20:
		fmt.Printf("%d küçüktür 20\n", x)
		fallthrough

	case x < 50:
		fmt.Printf("%d küçüktür 50\n", x) // fallthrough devam ettiren parametre
		fallthrough

	case x < 100:
		fmt.Printf("%d küçüktür 100\n", x)
		fallthrough

	case x < 200:
		fmt.Printf("%d küçüktür 200\n", x)

	} */

	/* *************************************************************** */

	/* x := 20
	if x%2 == 0 { // buranın açıklaması: x in 2 ye bölümünden kalan 0 ise
		fmt.Println(x, "çifttir")
		return // else görevi görür
	}
	fmt.Println(x, "tektir") */
	/* ************************************************************************************** */

	/* var x, y int
	for x = 2; x < 50; x++ {
		for y = 2; y < (x / y); y++ {
			if x%y == 0 {
				break
			}

		}
		if y > (x / y) {
			fmt.Printf("%d bir asal sayıdır\n", x) */ // bundan bi sik anlamadım.
}
