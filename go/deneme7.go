package main

func deneme7() {

	/* x := 75
	var y float64
	y = float64(x) // type conversion yapılmış

	fmt.Println(y) */
	/* ************************************************************************************************* */
	/* x := 31
	y := 52

	fmt.Println("X:", x, "Y:", y)

	x, y = y, x // burada x değeri 31 iken 52 ye dönüşüyor y değeri 52 iken 31 e dönüşüyor bu metot ile yerleri değiştirebiliriz.

	fmt.Println("X:", x, "Y:", y)
	*/
	/* **************************************************************************************************************** */

	/* x := 5

	if true {
		x := 10        // eğer sadece =10 yazsaydık yukarıdaki 5 i 10 a çeviricektik
		x++            // x i 1 arttırdık
		fmt.Println(x) // ilk önce if çalışır ve parantez dışındaki print en son çalışır

	}
	fmt.Println(x) // eğer if bloğunda := yerine sadece = kullanılsaydı yeni değer atamış olucaktım. fakat sonuçlar 5 ve 11 gelecektir. */

	/* ****************************************************************************** */

	/* x := 65

	s := string(x)

	fmt.Printf("%v , %T\n", x, x) //65

	fmt.Printf("%v , %T\n", s, s) // A
	y := strconv.Itoa(x)
	fmt.Printf("%v , %T\n", y, y)
	*/

	/* x := 60

	x += 10 // bunun cevabı 70 olarak gelecek
	x *= 2  //bunun cevabı ise 120 gelecektir kısa yollu işlemler böyle yapılıyor eşittir öncesine operatör koyuyoruz. */
}
