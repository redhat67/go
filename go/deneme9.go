package main

func deneme9() {

	/* x, y := 15, 20 */
	/* set1 := (x == y)
	set2 := (x < y) */

	/* set3 := true

	/* fmt.Printf("%T, %v\n", set1, set1)                     // yanlış ver,yor çünkü eşit değiller.
	fmt.Printf("%T, %v\n", set2, set2)                     // doğru veriyor çünkü y büyük. */
	/* fmt.Printf("%T, %v\n", (set1 && set2), (set1 && set2)) */ // şartların içinde false varsa her zaman && operatörü false gelir.
	/* fmt.Printf(" %v\n", (set1 || set2))                          // || bu operatör de 2 durum da false ise false gelmektedir.
	 */
	/* fmt.Printf(" %v\n", (!set3)) */ // true ise false gelecek false ise true gelecektir. */
}
